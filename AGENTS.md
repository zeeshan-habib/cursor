# AGENTS

## Cursor Cloud specific instructions

This repository is currently an empty scaffold (single `README.md`, no application code). There are no services, dependencies, build steps, lint checks, or tests to run. Once application code is added, update this section with relevant startup, testing, and development instructions.
